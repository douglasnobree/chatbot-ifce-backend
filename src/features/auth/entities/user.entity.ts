export class User {
  id: string;
  email: string;
  firstName: string;
  lastName: string;
  picture?: string;
  accessToken: string;
  isAtendente?: boolean;
  atendenteId?: string;
  departamento?: string; 
}
